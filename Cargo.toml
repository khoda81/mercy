[package]
name = "mercy"
version = "0.1.0"
edition = "2021"
description = "POC: a 512-bit interleaving of symbol and radix-256 boundaries for codec/model composition"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
num-bigint = "0.4"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "partition"
harness = false

[[bench]]
name = "reference_codec"
harness = false
